# v0.5.14 packed release verification

**English** | [简体中文](./README.zh-CN.md)

Tested revision: `07dac3d3d2d029ce3e4610226d40a757509d6dc7`, on 2026-09-25. The later evidence-only commit does not change any packed package input. [verification.json](./verification.json) records all seventeen artifact integrities, screenshot hashes and the exact Runtime content hash.

The release worktree consumes the fixes merged as #277, #276 and #278. It versions only the Starter to 0.5.14, OpenViking Provider to 0.5.6 and Memory Spaces Source to 0.5.9.

## Checks

- Node 24.20.0 / pnpm 10.13.1: full `pnpm verify` passed, with 1,360 root tests / 7 skips and 402 workspace-plugin tests / 2 skips. Both opt-in official Native CLI 0.2.9 tests ran, including two same-View archive destinations. Live-model, codec/cohort opt-ins and live-service/platform cases account for the remaining skips.
- Serial `pnpm verify:plugins --skip-build` passed for sixteen independent plugin repositories and seventeen packed artifacts, the external SDK consumer and real DSH Starter activation with all three optional Strategies. Worker counts were bounded; test and performance thresholds were unchanged.
- `release:status`, `release:version`, `release:check` and `release:intent` passed. The lockfile was regenerated by pnpm 10.13.1 without adding, removing or advancing third-party package versions.
- Before versioning, the integrated fix branch passed 99 targeted tests plus both official Teams matrices: sixteen combinations on 0.1.7-rc.1 and sixteen on the legacy cohort. See the [issue #275 record](../issue-275-agent-teams/README.md) for expected legacy refusals and scope limits.

## Real WebUI

The existing [packed Team fixture](../../../scripts/fixtures/serve-team-review.mjs) ran all seventeen release-version tarballs on an isolated official DSH/Teams 0.1.7-rc.1 Profile with the official CLI and all three Strategy enhancements enabled. The fixture uses disposable storage and a loopback model endpoint; no external model API was used.

1. Opened Memory System and observed `dsh-mnemon 0.5.14` with healthy status.
2. Added a synthetic Runtime entry through the UI, then edited it and observed the exact saved content.
3. Changed Agent Teams from the default `pause` to `scoped` through Settings and observed the saved/live status.
4. Stopped and restarted the Host process through the fixture's `SIGUSR2` cold-restart handler, then reopened the authenticated UI. Runtime content remained visible and its file SHA-256 was byte-identical. Settings still selected `scoped`, and all three Strategy enhancement checkboxes remained enabled.

| Observation | Screenshot |
|---|---|
| Runtime added before editing/restart | [runtime-saved.png](./runtime-saved.png) |
| Version and health after cold restart | [status-after-restart.png](./status-after-restart.png) |
| Edited Runtime content after cold restart | [runtime-after-restart.png](./runtime-after-restart.png) |
| Scoped review setting after cold restart | [settings-after-restart.png](./settings-after-restart.png) |

The optional source-link fixture on the pinned DSH 0.1.5-rc.1 development profile failed to load its `dsh-client-hmr` bundle. Its cause was not established. The packed 0.1.7-rc.1 installation passed without modifying official DSH code; this record does not claim that the source-link failure was fixed. The release smoke does not repeat live cloud-account or real-model acceptance. The original issue records preserve the broader before/after tests.
